import {
  ApplicationState,
  ActionProps,
  PayloadProps,
  AppDispatch,
} from '../types';

import {write} from '../actions';

export const initialise = () => {
  return async (dispatch: AppDispatch) => {

  };
};
